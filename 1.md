核心判断：fixed-1k 下 C++ 已经不落后，IPC 优化不该继续围绕“打赢 Go”做，而要压缩这条机制链，尤其解决大响应、小 credit window、streaming 和高并发下的放大效应。

  当前链路是：

  Asio IO
    → command mutex/queue
    → wake pipe
    → worker thread
    → Runtime encode/copy/flush
    → Unix SOCK_STREAM
    → worker parse/JS
    → Unix SOCK_STREAM
    → Runtime parser/copy
    → worker event queue
    → asio::post
    → Beast write
    → response credit 原路返回

  先纠正一个事实：src/host/single_worker_server.cc:1735 目前每个 event 都调用一次 asio::post，并不是报告所说的“仅 empty→non-empty 时 post”。这是第一项明确可做的机制优化。

  ## 给 DeepSeek 的批量任务

  ### M1B.1：先关闭证据缺口

  一次批量完成：

  - headline 顺序改为 A→B / B→A / A→B，消除固定后跑 C++ 的顺序偏差。
  - loadgen 增加明确的 measured-start/measured-end 信号，让 perf、IPC counters、RSS/PSS 使用同一窗口。
  - 修复 perf 符号解析；零用户态符号不能标 complete。
  - manifest 补齐 report、ready records、perf-stat、组件日志 SHA。
  - test_mode:true 标为 diagnostic，不能叫正式 complete。
  - 收紧 Go late-credit：只有已观察到 RESPONSE_END 才能忽略 INVALID_ARGUMENT。
  - 默认开启 TCP_NODELAY；精确 CAPSID_TCP_NODELAY=0 才关闭。
  - loadgen 发送固定显式 headers；fixture 返回 worker 实际观察到的 method、URL、headers 摘要，两侧逐字节一致。
  - 清理根目录文件 0，增加 runner 不污染工作目录的测试。

  交付两次独立 fixed-1k 和 CPU-template 运行，每次至少三轮。

  ### M1C-IPC0：建立 IPC 机制基线

  不要先优化，先接通已有的 src/client_ipc_metrics.h:15，补充 Host/worker counters：

  - frames/request、bytes/request；
  - socket read/write syscalls；
  - frames/syscall；
  - flush/request、EAGAIN；
  - command wakeups、commands/batch；
  - worker events、asio::post 次数、events/post；
  - parser payload copied bytes；
  - response credit updates、bytes/update；
  - credit stall 次数和持续时间；
  - command enqueue → IPC flush；
  - IPC readable → response head；
  - response head → client write complete；
  - queue high-water、RSS/PSS。

  只在 diagnostic run 开启，headline 必须零探针。

  测试矩阵：

  - fixed-1k：1/1、16/64；
  - CPU-template：16/64；
  - streaming：16KiB、80KiB、1MiB；
  - response chunk：1KiB 和 16KiB；
  - window：1KiB、16KiB、64KiB、256KiB；
  - 正常客户端与 slow client；
  - cancel/disconnect/timeout。

  特别重要：1KiB window 是信用机制压力测试，不应该直接代表生产默认值。80KiB × 1KiB window 天然会产生大量 credit 往返。

  ### M1C-IPC1：三个独立低风险实验

  每项单独 RED → GREEN → A/B，不允许合并实验。

  1. Event post 合并

  当前每个 RESPONSE_HEAD/BODY/END 都 post。改为：

  - events_ 从空变非空时才 asio::post；
  - pump_events() 一次 swap 全部；
  - post 执行期间新事件不会丢失唤醒；
  - 记录 events/post。

  预期：每响应约 3–4 次 post 降为每个 IPC read batch 约 1 次。

  2. Command 批量 flush

  当前 worker thread 虽然一次 swap 多条 command，但 src/host/single_worker_server.cc:1572 每条 command 都调用 capsid_worker_flush()。

  改成：

  - 批量 queue 多条 command；
  - 队列容量允许时只在 batch 尾部 flush；
  - WOULD_BLOCK 时 flush、等待 writable、从未消费 command 重试；
  - shutdown/cancel 可以打断；
  - 保持严格顺序和 fail-closed。

  重点观察 flush/request、send syscall/request、command latency。

  3. Response credit 聚合

  当前每次客户端 body write 完成就可能生成一次 WINDOW_UPDATE。改为：

  - write 成功后只累计 pending_credit；
  - 达到 low-water/批量阈值再提交；
  - credit 即将耗尽时立即提交，禁止制造死锁；
  - response end/cancel 丢弃未需要的 credit；
  - 慢客户端仍然只在真实写成功后归还；
  - 不允许累计 credit 超过已写出字节。

  建议先测试：

  window=64KiB，grant threshold=16KiB
  window=256KiB，grant threshold=64KiB

  不要直接冻结默认值，按 window sweep 决定。

  ### M1C-IPC2：bodyless 请求融合

  GET/HEAD 无 body 当前仍有 RequestHead + RequestEnd 两个 frame，worker 还可能发送无用的 request credit。

  候选协议：

  - RequestHead 增加 END_REQUEST flag；
  - worker 收到后直接标记 request ended；
  - bodyless 请求不发送 request-direction initial credit；
  - Host 一次 frame、一次解析，省掉两个控制 frame 的至少一个。

  这涉及协议版本/compatibility identity，必须新增协议负控和旧版本拒绝测试。不要偷偷重写现有 frame。

  ## 后续优化候选，必须由 profile 触发

  优先级由高到低：

  1. Parser/事件 payload 少拷贝
     当前 parser 从内部 buffer assign 到 payload，src/protocol.cc:134至少有一次完整复制，Host 跨线程又复制一次。可以先做稳定 buffer view/owned-event API，再考虑更大改造。

  2. 批量 next_event API
     一次 Runtime 调用取出一批完整 frames，降低 C ABI 循环、分配和状态检查成本。

  3. IO 与 worker 同 owner loop
     让 shard event loop 直接拥有 worker fd，移除 mutex command queue、wake pipe、专用 poll thread 和反向 asio::post。阻塞 destroy 仍交给 reaper。这是收益可能最大的结构性实验，但风险也最高，应在前三项之后
     做。

  4. writev/sendmsg
     header 与 payload 分散写，减少 encode buffer copy。只有 copied-bytes/profile 明确成为热点才做，因为 partial write 状态机会复杂不少。

  5. Shared-memory ring
     只在 1MiB+ streaming 表明 copy/Unix socket 已成为 dominant cost 时考虑。必须保留 credit、bounded queue、worker crash recovery 和 protocol validation。

  暂不考虑：

  - io_uring；
  - SOCK_SEQPACKET；
  - Linux-only eventfd 替换 wake pipe；
  - 无界 batching；
  - 为 QPS 提前归还 credit。

  这些要么收益不明确，要么破坏 macOS/未来 Windows 的 transport 抽象。

  ## 每项优化的验收门

  - 普通、ASan、UBSan；涉及队列/线程的实验增加 targeted TSan。
  - cancel、timeout、disconnect、partial write、EAGAIN、worker exit 全部回归。
  - 两次独立 benchmark，每次三轮并交替顺序。
  - mechanism counter 必须明显下降，例如 posts/response 或 credit frames/response 至少下降 20%。
  - 同时要求 QPS 提升至少 5%，或单连接 p50 改善至少 10%；否则不为复杂度合入。
  - p99、错误率、RSS/PSS、slow-client 行为不能回归。
  - profile 必须证明被优化路径的成本确实下降。

  建议实际执行顺序：证据修复 → IPC counters → event post 合并 → command flush 合并 → credit 聚合 → bodyless frame 融合。前三个机制实验做完，就能判断是否值得进入同 owner loop，而不是直接跳到共享内存。
